paths:
  processed: data/processed
  embeddings: data/embeddings
  index: data/index
  user_prefs: data/user_prefs/user_prefs.json 

data:
  primary_csv: data/processed/games_march2025_cleaned.csv

embedding:
  model: distiluse-base-multilingual-cased-v2
  batch_size: 256

index:
  backend: faiss

app:
  topk: 10
